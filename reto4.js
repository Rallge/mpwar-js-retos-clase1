/* Escribe una función que reciba dos enteros como
parámetro y devuelva un array con todos los
valores impares que hay entre ellos, no incluidos
• Si el primero es mayor al segundo, debe dar un
error
• Si no hay resultados debe retornar un array
vacío */

const factorial = function fac(n) {
  return n < 2 ? 1 : n * fac(n - 1);
};

console.log(factorial(3));

const devuelve_array = function dev(a,b){
    n = 0;
    if (n>a || n<b){
        return 
    }if (n<a || n>b){
 console.log("" )
    }
}

console.log(devuelve_array(3,6))

//redux